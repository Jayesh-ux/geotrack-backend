services:
  - type: web
    name: geotrack-backend
    env: node
    plan: free
    buildCommand: "npm install"
    # For free tier, we combine migration, seeding, and start in one command
    startCommand: "node scripts/runMigration.js && node utils/seedPincodes.js && npm start"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: geotrack-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production

databases:
  - name: geotrack-db
    databaseName: geotrackdb
    user: geotrackdb_user
    plan: free
    postgresMajorVersion: 15
